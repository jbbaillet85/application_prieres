/**
 * ============================================================
 * Application Prières
 * ============================================================
 * Point d'entrée de l'application.
 */

'use strict';

/**
 * Initialise l'application.
 */
function init() {
    console.info('🙏 Application Prières initialisée');
}

/**
 * Attend que le DOM soit entièrement chargé.
 */
document.addEventListener('DOMContentLoaded', () => {
    init();
});